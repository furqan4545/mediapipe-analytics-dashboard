"use client"

/**
 * Placeholder kept for forward compatibility — we may bring back a creators/
 * automations toggle in v2. For now it renders nothing and has no deps on
 * the deleted useAdminProject hook.
 */
export function SourceToggle() {
  return null
}
